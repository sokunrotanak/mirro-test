pages:
  stage: deploy
  script:
    - mkdir .public
    - cp -r * .public
    - mv .public public
  artifacts:
    paths:
      - public
  only:
    - master

test:
  stage: test
  script:
    -  echo "only pull request"
  only:
    - external_pull_requests
